{
  "name": "dpv-booking",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute dpv-booking --local --file=migrations/0001_schema.sql",
    "db:init:remote": "wrangler d1 execute dpv-booking --file=migrations/0001_schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
